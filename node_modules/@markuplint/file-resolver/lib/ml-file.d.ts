export declare class MLFile {
    #private;
    readonly anonymous: boolean;
    /**
     *
     * @param filePathOrContext A file path or context
     * @param anonymous if 1st param is a context
     * @param workspace context of workspace if anonymous is true
     * @param name context of name
     */
    constructor(filePathOrContext: string, anonymous?: boolean, workspace?: string, name?: string);
    get path(): string;
    isExist(): Promise<boolean>;
    getContext(): Promise<string>;
    matches(globPath: string): boolean;
    private _fetch;
}
