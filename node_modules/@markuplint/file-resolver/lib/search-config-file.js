"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.searchConfigFile = void 0;
const get_config_file_1 = require("./get-config-file");
const helper_1 = require("./helper");
/**
 * Asynchronously search configuration file from linting target file.
 *
 * @param baseDirOrTargetFilePath A path of linting target file. To be base directory for searching. (ex. `"path/to/target.html"`)
 * @param recursiveExtends Recursive load by extends options.
 * @param cacheClear Clear cache when loading.
 */
async function searchConfigFile(baseDirOrTargetFilePath, recursiveExtends = true, cacheClear = false) {
    const result = await helper_1.search(baseDirOrTargetFilePath, cacheClear);
    if (!result) {
        return;
    }
    return get_config_file_1.getConfigFile(result.filePath, result.config, recursiveExtends, cacheClear);
}
exports.searchConfigFile = searchConfigFile;
