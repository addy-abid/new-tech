import { Config, RuleConfigValue } from '@markuplint/ml-config';
import { MLRule } from '@markuplint/ml-core';
export { lint as exec } from './lint';
/**
 * @deprecated
 * @param html
 * @param config
 * @param rules
 * @param locale
 */
export declare function verify(html: string, config: Config, rules: MLRule<RuleConfigValue, unknown>[], locale?: string): Promise<import("@markuplint/ml-config").VerifiedResult[]>;
/**
 * @deprecated
 * @param html
 * @param config
 * @param rules
 * @param locale
 */
export declare function fix(html: string, config: Config, rules: MLRule<RuleConfigValue, unknown>[], locale?: string): Promise<string>;
