import { ConfigSet, MLFile } from '@markuplint/file-resolver';
import { Config } from '@markuplint/ml-config';
export declare function resolveConfigs(files: MLFile[], options: {
    config?: string | Config;
    workspace?: string;
    defaultConfig?: Config;
}): Promise<Map<MLFile, ConfigSet>>;
